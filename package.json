{
  "name": "signal-resolution",
  "version": "1.0.0",
  "description": "Signal Resolution — B2B SaaS GTM diagnostic and intelligence platform",
  "scripts": {
    "start": "eleventy --serve --watch",
    "build": "eleventy",
    "debug": "DEBUG=Eleventy* eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "luxon": "^3.4.4",
    "markdown-it": "^14.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
